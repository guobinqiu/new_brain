[project]
name = "brain-llm"
version = "0.1.0"
description = "RAG LLM backend"
requires-python = ">=3.12"
dependencies = [
    "langgraph",
    "langchain",
    "langchain-openai",
    "python-dotenv",
    "langgraph-checkpoint-postgres",
    "psycopg[binary]",
    "psycopg-pool",
    "fastapi",
    "uvicorn",
    "tenacity",
    "structlog",
    "pydantic-settings",
    "slowapi",
    "httpx",
    "pyyaml",
    "langsmith",
    "PyJWT",
    "pypinyin>=0.50.0",
]

[dependency-groups]
dev = [
    "pytest",
    "pytest-asyncio",
    "ruff>=0.6",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["."]
